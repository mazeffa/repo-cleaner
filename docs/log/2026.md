> **Format for new entries** (append-only): `## YYYY-MM-DD [(N)] — headline`, `(N)` only
> when more than one entry shares a date. Then `Decisions:` (D-NNN this entry touched, or
> `none`) and `Docs:` (files changed), plus `State:` on this file's *last* entry only — one
> line: what's true now, what's pending. Current state is always
> `grep '^State:' docs/log/2026.md | tail -1`.

# 2026 log

## 2026-09-18 — Published, reviewed, and made honest

Decisions: D-001, D-002, D-003, D-004, D-005, D-006
Docs: CLAUDE.md, README.md, docs/DECISIONS.md, skills/repo-clean/SKILL.md,
skills/repo-clean/init.md, skills/repo-clean/maintain.md,
skills/repo-clean/scripts/check_docs.py

**What happened.** Scrubbed identity and a private project's content out of the plugin,
published it to github.com/mazeffa/repo-cleaner, and fixed ten review findings across two
rounds. Then three rounds of external design review on a proposed rewrite, which converged
on: the checker enforces shape and cannot touch content, so ship the linter and stop
pricing hygiene as governance. Applied the parts of that verdict that fit the existing
plugin rather than starting the rewrite.

## 2026-09-18 (2) — v0.2.1: clean-run message, stale-claim sweep

Decisions: none
Docs: README.md, docs/log/2026.md, skills/repo-clean/scripts/check_docs.py

**What happened.** A clean run printed nothing, so silence read as "the docs are correct";
it now prints "Shape verified; content not verified." Then a sweep for claims that this
work had made untrue: the module docstring still said "Exit 0 and silent on success", and
`CORE_VERSION` was still 1.0.0 despite a new rule, a parser fix and changed output — which
would have made the version-drift check in `maintain` step 7 report "same version" for two
different cores. Decided against a CI pipeline: the pre-commit hook covers this repo, and a
per-repo pipeline file is upkeep for a gap that only opens with a second contributor.

## 2026-09-18 (3) — Adversarial pass: 22 defects, none caught by the selfcheck

Decisions: D-007
Docs: CLAUDE.md, docs/DECISIONS.md, docs/DEFECTS.md, docs/log/2026.md

**What happened.** Everything shipped so far was verified against inputs chosen to pass:
the bundled selfcheck, one hand-built repo, and one banned-header commit. A pass built to
break it instead found 22 defects, four reproduced directly here. Two block use: the
checker crashes on any repo path containing a non-ASCII character, and it reports a missing
path for any backticked command carrying a flag — including the command this repo's README
tells users to run. The documented per-rule disable does not exist, so there is no way to
switch that off. The hook installed in this repo is mode 644, so its "blocks a bad commit"
claim holds on Windows only.

The selfcheck caught none of them, and that is structural: its fixtures are written by the
same hand as the rules, so each proves its rule can fire and none proves a rule stays quiet
when it should.

## 2026-09-19 — All 22 defects fixed; hooks make logging and the checker automatic

Decisions: D-008
Docs: .claude-plugin/plugin.json, CLAUDE.md, README.md, docs/DECISIONS.md, docs/DEFECTS.md,
hooks/hooks.json, hooks/_lib.py, hooks/session_start.py, hooks/post_tool_use.py,
hooks/stop.py, hooks/pre_compact.py, skills/repo-clean/SKILL.md, skills/repo-clean/init.md,
skills/repo-clean/maintain.md, skills/repo-clean/example-layout.md,
skills/repo-clean/scripts/check_docs.py, scripts/tools/check_docs.py,
scripts/hooks/pre-commit

**What happened.** Rewrote `check_docs.py` (CORE_VERSION 1.2.0) around a fence-aware
document model (`read_doc`/`parse_fields`/`split_entries`), a stricter path grammar
(`exists_exact` for case- and directory-correctness, per-token candidate matching instead
of whole-line regexes), a hardened `argparse` CLI, and a working per-rule `CONFIG[id] =
None` switch. All 22 defects in DEFECTS.md are fixed; selfcheck is now 50 fire/stay-quiet
pairs instead of 37 fire-only assertions. Fixed the pre-commit hook's mode bit (was 644,
silently skipped on Unix).

Added SessionStart/PostToolUse/Stop/PreCompact hooks (`hooks/`) so setup, logging, and the
checker gate no longer depend on a model remembering to run them: Stop forces this session's
log entry to exist and the checker to pass before Claude can go quiet on a session that
changed files; PreCompact folds pending edits in before context is lost. Git is now
optional throughout — `init` only installs the pre-commit hook and runs
`core.hooksPath` when `.git` exists. D-008 supersedes D-002 (which had rejected a
session-end gate as unenforceable; a real Stop hook is not that).

Deferred, not done this session: applying the same checker upgrade to the draftkings-nfl
model repo, and the multi-round external adversarial-debate pass against another model —
both need a separate go-ahead since they touch a second repo and spawn another model.

## 2026-09-19 (2) — Review found 4 hook bugs, one of which had already corrupted this file

Decisions: none
Docs: docs/log/2026.md, hooks/_lib.py, hooks/stop.py, skills/repo-clean/SKILL.md

**What happened.** A review of the (2) entry's hooks (not run against a live session, only
scratch copies) found four real bugs. `_strip_last_state` deleted only a wrapped `State:`
line's first physical line, orphaning its continuation as bare prose with no `State:`
prefix — the checker can't see this because orphaned prose is valid shape, and it had
already happened to the (3) entry above when its `State:` was manually moved off. Fixed by
reusing the continuation-following logic `last_state_line` already had, factored into
`_field_span`, and used to delete the whole field in `_strip_last_state` too. Restored (3)'s
orphaned line. `add_file` recorded `docs/log/*.md` itself as a changed file, so filling in
the entry (an edit to the log) fed the next Stop call, which folded the log into its own
`Docs:` line — excluded `docs/log/` from `add_file`. `run_checker` called `python`
literally; on a box without that alias (common on Linux/Mac, where it's `python3`) the Stop
hook would crash and Claude Code would fail open silently — switched to `sys.executable`.
Stop had no retry cap: a finding Claude can't fix (e.g. a missing decisions file) blocked
every future Stop forever, and the message always said "fill in the headline and State:"
even when only a checker finding remained. Fixed: the message now names which condition
actually failed, a per-session retry counter caps it at 3, and the fourth attempt fails
open with a warning instead of wedging the session shut.

State: v0.3.0. All 22 DEFECTS.md entries fixed; checker at CORE_VERSION 1.2.0, selfcheck 50
findings exit 0. Hooks fixed after review (wrapped-State deletion, self-reference loop,
retry cap, sys.executable) and re-verified against scratch copies, but still not exercised
in a live Claude Code session — first real session should confirm Stop actually blocks and
SessionStart actually fires. Decided D-009: the repo's own docs stay public. Open: D-006. Next: draftkings repo upgrade and the adversarial
debate pass, if wanted.

## 2026-09-19 (3) — Hooks hardened and given a self-test; still unreleased, still not live-tested

Decisions: none
Docs: docs/log/2026.md, hooks/_lib.py, hooks/post_tool_use.py, hooks/pre_compact.py,
hooks/selftest.py, hooks/session_start.py, hooks/stop.py, scripts/hooks/pre-commit,
skills/repo-clean/SKILL.md, skills/repo-clean/scripts/pre-commit

**What happened.** Ran the code-only parts of the "ship 0.3.0, live-test, then harden"
plan: the hardening (step 3) and the hook self-test (step 4). Steps 1, 2, and 5 — pushing,
installing the plugin, restarting Claude Code, and running the live session rows a–g — need
the user's own machine and an actual session restart, which this session can't do from
inside itself; they're still outstanding. `add_file` now stamps `session["cwd"]` via a new
`normalize_cwd()` (`os.path.normcase(os.path.realpath(...))`). `session_start.py` does one
pass over `DATA_DIR` per session: a same-cwd orphan (non-empty `files` or `pending`) older
than 2 hours is merged into the current session and deleted, printing "Carried N unlogged
changes"; anything older than 7 days is pruned outright, regardless of cwd. The setup nag
is now recorded per normalized cwd in an `offered.json` next to the session data, and
fires once.
Stop's give-up message now says "entry passed unverified" so the retry cap reads as a
safety valve, not a pass. `DATA_DIR`/`OFFERED_FILE` moved under a `BASE_DIR` that honors
`REPO_CLEAN_HOME`, so the self-test never touches a real user's session data.

`hooks/selftest.py` (stdlib only) copies this repo into a temp dir named with both a
non-ASCII character and a space, then drives all four hooks by subprocess and the
pre-commit script directly, asserting the behaviors listed in the plan (recording,
dedup, ignoring `docs/log/`, blocking/passing/merging/retry-capping in Stop, entry
creation and merging in PreCompact, the State: print/setup-offer/orphan/prune matrix in
SessionStart, and a banned-header commit failing pre-commit). It found and fixed one real
bug along the way: all four hooks crashed with an uncaught `JSONDecodeError` (exit 1, a
traceback on stderr) on empty or malformed stdin instead of exiting 0 as required — fixed
with a shared `read_stdin_json()` in `_lib.py` that treats bad input as an empty payload.
`python hooks/selftest.py` now runs from `scripts/hooks/pre-commit` (and the shipped
`skills/repo-clean/scripts/pre-commit` template, guarded by `-f hooks/selftest.py` so it's
a no-op in installed target repos, keeping the two files byte-identical).

## 2026-09-19 (4) — Live-test rows 1–4 pass in a real session; one Stop false-positive fixed

`claude plugin update repo-clean` installed 0.3.0 (cache identical to the repo except CRLF)
two minutes after a session had already started on the 0.2.2 manifest, so that session saw
no hooks — expected, not a bug. After restart: SessionStart printed the State: line verbatim
(row 1); a read-only turn did not block (row 2); an Edit-tool change to `hooks/_lib.py`
was recorded by PostToolUse in the per-session JSON under the hooks' data dir and Stop then
created this entry with placeholders and blocked, naming the line (row 3). The sessions dir
is legitimately empty after SessionStart alone — it only writes when carrying orphans.

Live bug: after the entry was filled, Stop kept blocking, because `entry_has_placeholder()`
tested for the placeholder word anywhere in the entry, and the body described the
placeholders it had just replaced. Fixed to test only the heading suffix and the `State:`
value, with a selftest assertion (a body naming the placeholder must not block). The fix lives in
the repo, not the running cache copy, so the entry body here avoids the literal word.

Also added to `read_stdin_json()`: every hook payload is dumped to
`capture/<event>-<ms>.json` under the hooks' data dir when that directory exists (mkdir to
turn on, rmdir off; no env var, so no restart). Live only after the next plugin update; the
dir was not created this session (the auto-mode classifier denied it as persistence).
Bash-driven edits (`python - <<EOF`, sed) do not fire PostToolUse — only the Edit/Write
tools do, which is what made row 3 work when a heredoc edit had been denied anyway.
Row 5: the user ran `/compact`; PreCompact exited 0 and left this entry as the only (4)
heading with its Docs: line unchanged. It had nothing new to fold (files were already
logged), so the fold-in path itself is still only asserted by the selftest.

After pushing, `claude plugin update repo-clean` and a restart, 0.3.1 was live. (A first
read of the capture timestamps credited `/reload-plugins` with rebinding hooks; the user
had restarted, which the session can't observe. Restart remains the rule.) Real SessionStart, Stop and PostToolUse payloads are now in
`hooks/fixtures/` (message body and file contents trimmed), and `hooks/selftest.py`
overlays every test's fields onto the matching fixture so all assertions run against
the real key set. The payloads also showed Claude Code reporting `cwd` with different
drive-letter case across events in one session, which is what `normalize_cwd` guards.
A second `/compact` with the capture dir present gave the PreCompact payload (`trigger`,
`custom_instructions`, no session-state fields), so all four hooks now run under real fixtures.

Fresh-folder rows 6 (git) and 7 (no git) ran headlessly (`claude -p` in two temp folders, two
Sonnet subagents driving the edit sessions, `init` run in a follow-up `-p` session). Both
folders: SessionStart fired on `startup` and marked the cwd in `offered.json` on the first
session only; PostToolUse recorded the Write; Stop was a no-op until a doc system existed,
then created an entry the session filled, and the record reset (pending false, retries 0);
checker exit 0 on the scaffold. Git folder: `pre-commit` blocked a `## TODO` commit with two
findings. Two caveats. Headless output hides hook stdout, so the offer text and `State:` print
are inferred from `offered.json` and the capture files, not seen; both subagents misread the
pre-marked cwd as an earlier run when it was their own denied first session. And `-p` needs
`--permission-mode acceptEdits` plus `--add-dir` on the plugin cache or the skill can't read
its own `init.md`; interactive sessions don't have that problem.

Decisions: D-009
Docs: .claude-plugin/plugin.json, docs/DECISIONS.md, hooks/_lib.py, hooks/fixtures/PostToolUse.json,
hooks/fixtures/PreCompact.json, hooks/fixtures/SessionStart.json, hooks/fixtures/Stop.json, hooks/selftest.py

## 2026-09-19 (5) — `CORE_VERSION` doesn't identify a build; drift detection is a no-op

A user comparing shipped releases in the plugin cache against a target repo's installed
copy found four releases claiming `CORE_VERSION = "1.0.0"` with different code, one of them
carrying a rule the other three lack. Logged as DEF-23 — the first open defect since the
2026-09-18 pass was closed, and the first found from outside this repo. Nothing is fixed
yet; the evidence, the repro and four fix directions are in `docs/DEFECTS.md`.

Decisions: none
Docs: docs/DEFECTS.md

## 2026-09-19 (6) — DEF-23 closed: a build digest in `--version`, now actually used

Fix direction (1) of DEF-23 was implemented unasked in both checker copies — built in answer
to a yes/no question about whether the fix was straightforward. The user chose keep and
finish rather than revert. Finishing meant the digest had to actually be used and no doc
could contradict the code, so this landed as one commit: `maintain.md` step 7 now compares
the full `--version` line, digest included, instead of version strings alone, and step 7
also flags a target that prints no `+digest` as drift (it predates this check). `SKILL.md`'s
`--version` description now mentions the digest. The dead CRLF-independence selftest
assertion (re-normalizes and hashes, which is what `core_digest` already does, so it could
not fail) was dropped; the 8-char shape assertion stays. Both checker copies remain
byte-identical. DEF-23 moved to `docs/DEFECTS.md`'s Fixed section in Was:/Fix: shape; its
other three fix directions are recorded there as improvements, not open work.

No `CORE_VERSION` bump: the digest is now the identity mechanism, so bumps stop being it.
Released as plugin 0.3.2 (the skill's checker copy changed, and installs only refresh on a
`plugin.json` bump). The user had to ask for the release; the first pass skipped it.

Decisions: none
Docs: scripts/tools/check_docs.py, skills/repo-clean/scripts/check_docs.py, skills/repo-clean/maintain.md, skills/repo-clean/SKILL.md, docs/DEFECTS.md

## 2026-09-19 (7) — Bump to 0.3.2

`plugin.json` 0.3.1 → 0.3.2 so installs pick up the DEF-23 digest; installs only refresh on
a version bump, and the (6) commit had left the skill's checker copy changed without one.

Decisions: none
Docs: .claude-plugin/plugin.json

## 2026-09-19 (8) — DEF-23 finished: drift check runs every session

(6) closed the mechanism but overclaimed "fixed": nothing ran the `--version` comparison
unasked, so a repo three releases behind still kept a green Stop hook and green
pre-commit gate, with the only signal being prose that fires when a human types
`/repo-clean maintain`. 0.3.2 itself reproduced the defect's headline: it and 0.3.1 both
print `CORE_VERSION 1.2.0` with different bytes (`9bafef37` vs `dda3df32`).

`hooks/_lib.py` gets `checker_drift(cwd)`: hashes the target's checker and the plugin's
own copy directly (no dependence on the target's `--version` output, so it also works
against pre-0.3.2 targets), and reports direction from a parsed version compare — behind,
ahead, or same version with different bytes. `session_start.py` prints the message once
per distinct drift state as a finding for the user, not a command, using a new
`drift_unreported()` gate (same on-disk pattern as the existing setup-offer gate).
`stop.py` adds a short, ungated note to the block reason on every retry while drift
persists, since the SessionStart line goes quiet after the first sighting and a user who
ignores it would otherwise see nothing again — a deliberate cost, recorded in DEF-23 as
the price of a nag that can't be outwaited. `CORE_VERSION` bumped 1.2.0 → 1.3.0 with a
comment to bump on any content change, since 1.2.0 had already named two different
builds. Review feedback applied before landing: message phrased as a finding, direction
computed rather than just "different", suppression scoped to drift state rather than
one-shot, `checker_path()` reused for the plugin-side lookup.

`docs/DEFECTS.md` DEF-23 stays `fixed`; its Fix paragraph now describes the SessionStart
and Stop reporting and states plainly what remains true regardless: `run_checker()` still
executes whatever checker the target actually has, so a stale ruleset is what the gate
enforces until the user re-syncs it. Remaining-ideas paragraph keeps only the
`CONFIG_VERSION`-naming and offer-the-`cp` directions; the "run every session" direction
is done.

Decisions: none
Docs: hooks/_lib.py, hooks/session_start.py, hooks/stop.py, hooks/selftest.py, scripts/tools/check_docs.py, skills/repo-clean/scripts/check_docs.py, skills/repo-clean/SKILL.md, skills/repo-clean/maintain.md, docs/DEFECTS.md, README.md, .claude-plugin/plugin.json

## 2026-09-19 (9) — DEF-24/DEF-25 fixed: hooks stop corrupting their own log; shipped as 0.3.4

This replaces a previous (9) that was itself a hook bug, not a record of work: 0.3.3's Stop
hook, unaware that (8) had already been written by hand and pushed as `5796ba8`, appended a
duplicate entry with the same `Docs:` list. That duplicate was later filled in as a note
about itself. Deleting and replacing it here is a deliberate exception to the log's
append-only rule (per `CLAUDE.md`): a hook stub restating another entry is not a record of
work, and leaving both would make the *next* reader mistake this repair for the bug.

Validating `5796ba8` surfaced the same PostToolUse/Stop pair corrupting (7) too, live: while
this plan was in a file outside the repo, PostToolUse recorded that path "as given" and Stop
folded it into (7)'s `Docs:` line — restored above to just
`.claude-plugin/plugin.json`. Two fixes: `hooks/post_tool_use.py` now drops any path outside
the target repo instead of recording it unresolved (DEF-24). `hooks/_lib.py`'s
`ensure_entry()` now checks the log's last entry before stubbing: if it's dated today and
its `Docs:` set overlaps the session's files, the session adopts it (binds
`entry_date`/`entry_n`, merges the new files in) instead of creating a stub — closing the
path that produced the old (9) (DEF-25). Also hardened: `_drift_detail()` now guards its two
`read_bytes()` calls with `try/except OSError`, so a target whose checker path exists but
isn't a readable file (a directory, mid-write) can't crash SessionStart.

0.3.3 was installed partway through this work, so this session's own hook state was still
bound to (7) under the old logic. Mitigation: the session's bound file in the hooks' session
data directory (see `SKILL.md`) was removed as the last action after this commit and push,
so the installed 0.3.3 Stop hook has no session record to act on and cannot re-fold anything
into (7). `hooks/selftest.py` gained cases for both fixes and the guard: an out-of-cwd
`file_path` leaves `files` unchanged; a scratch repo whose log already ends with a
same-day, overlapping entry gets adopted rather than stubbed (and a non-overlapping same-day
entry still gets a stub); a checker path that's a directory doesn't crash SessionStart.

Decisions: none
Docs: .claude-plugin/plugin.json, docs/log/2026.md, docs/DEFECTS.md, hooks/post_tool_use.py, hooks/_lib.py, hooks/selftest.py, skills/repo-clean/SKILL.md
State: 0.3.4 pushed, not yet installed (`claude plugin update repo-clean` + restart pending); 0.3.3 installed; in-repo rows 1–5 pass, two fresh-folder rows remain. DEF-23 to DEF-25 fixed. Open: D-006.
